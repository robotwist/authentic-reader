#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running security checks before commit..."
node scripts/check-tokens.js

# Tests are currently disabled as there's no test script
# echo "🧪 Running tests..."
# npm test
